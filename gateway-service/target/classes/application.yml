server:
  port: 8080

spring:
  application:
    name: gateway-service

  data:
    redis:
      host: redis
      port: 6379

gateway:
  services:
    user-service:
      uri: ${USER_SERVICE_URI:http://user-service:8082}

jwt:
  secret: ${JWT_SECRET}
  expiration: 3600000